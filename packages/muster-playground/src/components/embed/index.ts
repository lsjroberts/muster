import { ContainerComponent } from '@dws/muster-react';
import { EmbedView } from './embed';
import { EmbedContainer } from './embed.container';

export const Embed: ContainerComponent = EmbedContainer(EmbedView);
